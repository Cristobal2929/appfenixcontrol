<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFenix">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"/>

        <!-- Ventana transparente que abre el micrófono por encima de
             cualquier app (burbuja flotante o doble pulsación de volumen). -->
        <activity
            android:name=".VoiceActivity"
            android:exported="false"
            android:theme="@style/Theme.AppFenix.Transparent"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"/>

        <!-- Servicio de accesibilidad: es lo que da el control de pantalla.
             El usuario debe activarlo a mano en Ajustes > Accesibilidad. -->
        <service
            android:name=".FenixAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

        <!-- Burbuja flotante: se enciende/apaga a mano desde el botón 🔥
             de MainActivity, pidiendo antes el permiso de superposición. -->
        <service
            android:name=".BotonFlotanteService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_voice_bubble"/>
        </service>

    </application>
</manifest>
